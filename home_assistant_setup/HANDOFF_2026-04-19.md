# Handoff — 2026-04-19

Continuing from session that ran in `D:\Claude\Projects\homeassistant-config`
without the HA MCP loaded. Next session should launch Claude Code from
`D:\Claude\Projects\Container Home` so the MCP in `.claude\mcp.json` is active.

## What exists now

### Repos
| Repo | Remote | Local clone |
|---|---|---|
| HA config | `github.com:Blaze350454/claude-code-ha_1.git` | `D:\Claude\Projects\homeassistant-config` |
| ESPHome config | `github.com:Blaze350454/esphome-config.git` | `D:\Claude\Projects\esphome-config` |

Both private. Both have CI (`.github/workflows/validate.yml`). HA CI is
**non-blocking** because HA 2026.4.0 has a `check_config` KeyError bug
(`helpers/trigger.py:205` — `KeyError: 'triggers'`). Revisit when HA upgrades.

### VMs
| VM | IP | Config path | Branch |
|---|---|---|---|
| HA Container | 192.168.2.151 | `/home/homeadmin/homeassistant/.config` | `main` |
| ESPHome | 192.168.2.14 | `/root/config` | `main` |

SSH keys already trusted for `homeadmin@192.168.2.151` and `root@192.168.2.14`.

### Deployment pipeline (live)
```
Windows edit → git push → GitHub Actions (CI) → VM git pull → HA restart
```

**Cron jobs on HA VM (`crontab -l` as homeadmin):**
- `0 2 * * *` — `backup-ha.sh` — pull-rebase → commit UI edits → push
- `0 3 * * *` — `ha_backup_helpers.sh` — back up UI-defined helpers
- `*/15 * * * *` — `pull-ha.sh` — pull; if HEAD moved, `docker restart homeassistant` (log: `/home/homeadmin/pull.log`)

**Cron on ESPHome VM:**
- `15 2 * * *` — `backup-esphome.sh` — pull-rebase → commit dashboard edits → push (log: `/root/backup.log`)

### HA package layout (post-split, commit 44aa795)
```
packages/
  grow_tent_package.yaml      # core grow tent (sensors, helpers, templates)
  grow_tent_irrigation.yaml   # irrigation helpers (input_datetime/number/boolean)
  tent/
    irrigation/  reservoirs.yaml schedule.yaml feed_cycle.yaml drainage.yaml safety.yaml
    lights/      lights.yaml
    climate/     ac.yaml humidifier.yaml dehumidifier.yaml vpd.yaml heat.yaml[placeholder]
    fans/        fans.yaml
    control/     day_night.yaml stage_changes.yaml safety.yaml
  printer/
    bambu/       bambu.yaml
    prusa/       (empty — placeholder)
  system/        alerts.yaml
```

`configuration.yaml` uses `packages: !include_dir_merge_named packages` so subdirs
merge. Each package file is wrapped under a unique top-level key (e.g.
`tent_irrigation_safety:`).

`automations.yaml` is now `[]` (kept so HA's `automation: !include
automations.yaml` line still parses). All 48 automations live in packages.

**Verified live in HA:** 48/48 automations loaded, on state, no config errors.

## What's left from the original plan

1. **Branch protection** on both GitHub repos
   - Require CI to pass before merging to `main`
   - Note: HA CI is non-blocking, so require only yamllint there; or wait for HA 2026.5+ to fix `check_config` then tighten

2. **Fill placeholders**
   - `packages/tent/climate/heat.yaml` — heating control automations (none yet)
   - `packages/printer/prusa/` — no Prusa automations yet

3. **Cosmetic cleanup** (low priority)
   - `grow_tent_package.yaml` has comments at col 0 between the `grow_tent:` wrapper key and content — ugly but functional

4. **Use HA MCP for future work** — the whole reason for the handoff. Session
   should launch from `D:\Claude\Projects\Container Home` so MCP tools
   (`ha_*`, doc search) are available for HA REST queries instead of
   curl-with-extracted-token hacks.

## What was verified this session

- 10 push/pull pipeline cycles (3s each, zero failures)
- 2 real end-to-end deploys (push → VM pull → HA restart → logs clean)
- `pull-ha.sh` behavior: silent exit when nothing to pull, restart + log when HEAD moved

## Gotchas worth remembering

- **HA config dir has a leading dot:** `/home/homeadmin/homeassistant/.config`, not `config`. Easy to SSH into the wrong path.
- **The 2 AM HA backup cron now pull-rebases first** (line 15 of `backup-ha.sh`) so it won't conflict with Windows pushes during the day.
- **Orphaned entity cleanup:** after deleting an automation, the entity stays in HA's registry as `unavailable` until removed via **Settings → Devices & Services → Entities**. (Cleaned up `automation.air_pump_controll` this session.)
- **GitHub repo name is `claude-code-ha_1`, not `homeassistant-config`.** The latter was deleted in the repo cleanup earlier this session.
