.env
.venv/
__pycache__/
*.pyc
storage/document_chunks_cache.pkl

# Credentials
.claude/mcp.json
.cursor/mcp.json
.mcp.json
Google Drive Credentials Client ID.json
Google Drive client_secret.json
Irrigation/esphome/secrets.yaml
Pics/Github access token.jpg
Pics/Backups Encryption Key.jpg

# Backups
*.tar
home_assistant_backup_emergency_kit_*.txt
dashboard_*_backup_*.json

# CAD STEP exports kept locally only - large, don't delta-compress, and the
# Fusion documents are the dimensional authority anyway. `git add -f` to
# override for a specific file. SVG/PNG drawings are still tracked.
*.step

# Claude temp files
tmpclaude-*

# Per-machine Claude Code state (not shared config)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Local skill symlinks (point to in-repo .agents/skills/)
.claude/skills/caveman
.claude/skills/flash-esp32
.claude/skills/update-homelab
